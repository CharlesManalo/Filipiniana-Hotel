'use client';
import {useEffect,useRef} from 'react';
export default function Reveal({children,className='',delay=0}:{children:React.ReactNode;className?:string;delay?:number}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const node=ref.current;if(!node)return;const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){node.classList.add('is-visible');observer.unobserve(node);}})},{threshold:.1});observer.observe(node);return()=>observer.disconnect();},[]);return <div ref={ref} className={`reveal ${className}`} style={{'--delay':`${delay}ms`} as React.CSSProperties}>{children}</div>}
export function Heading({label,title,description,center=false}:{label:string;title:React.ReactNode;description?:string;center?:boolean}){return <Reveal className={`section-heading ${center?'text-center mx-auto':''}`}><span className="eyebrow blur-item">{label}</span><h2 className="blur-item">{title}</h2>{description&&<p className="blur-item">{description}</p>}</Reveal>}
