'use client';
import Image from 'next/image';
import {ArrowUpRight} from 'lucide-react';
import {useHotel} from './HotelProvider';
import Reveal,{Heading} from './Reveal';
export default function Events(){const {content,openInquiry}=useHotel();return <section id="events" className="section container events-section"><div className="intro-row"><Heading label="THE PEOPLE. THE OCCASION. THE PLACE." title={<>Gather here.<br/><em>Make it meaningful.</em></>}/><div className="intro-side"><p>From new beginnings to big ideas, bring the essentials together: a venue, a shared meal, and a comfortable stay.</p><button className="text-button" onClick={()=>openInquiry({category:'Events'})}>Let’s plan your occasion <ArrowUpRight size={17}/></button></div></div><div className="event-grid">{content.services.filter(s=>['weddings','seminars','socials'].includes(s.id)).map((s,i)=><Reveal key={s.id} delay={i*80}><article className="event-card"><Image src={s.image} alt={s.alt} fill sizes="(max-width: 800px) 100vw, 33vw"/><div className="event-card-content"><span className="eyebrow">{['A BEAUTIFUL BEGINNING','A PLACE FOR POSSIBILITY','THE JOY OF TOGETHER'][i]}</span><h3>{['Weddings','Seminars & conferences','Socials & celebrations'][i]}</h3><p>{s.description}</p><button className="text-button light" onClick={()=>openInquiry({category:'Events',service:s.id})}>Request a proposal <ArrowUpRight size={16}/></button></div></article></Reveal>)}</div></section>}
