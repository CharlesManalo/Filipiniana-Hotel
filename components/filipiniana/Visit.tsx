'use client';
import {MapPin,ArrowUpRight,Phone,Mail} from 'lucide-react';
import {useHotel,track} from './HotelProvider';
import Reveal,{Heading} from './Reveal';
export default function Visit(){const {content,openPolicies}=useHotel();const h=content.hotel;return <section id="contact" className="section visit-section"><div className="container"><div className="visit-grid"><div><Heading label="CLOSE TO THE CITY. CLOSER TO YOUR PLANS." title={<>Your Calapan address.<br/><em>We’ll see you here.</em></>} description="Near Xentro Mall, behind Landbank Calapan, and ready to welcome you. Call, email, or message us to make your plans a little easier."/><address className="visit-address">{h.address}</address><a className="button" href={h.maps} target="_blank" rel="noreferrer" onClick={()=>track('map_click')}>Get directions <ArrowUpRight size={17}/></a><div className="visit-links"><a href="tel:+63432886624" onClick={()=>track('phone_click')}><Phone size={14}/>{h.phone}</a><a href={`mailto:${h.email}`} onClick={()=>track('email_click')}><Mail size={14}/>Email reservations</a></div></div><Reveal className="location-card"><div className="map-pattern"/><span className="location-marker"><MapPin size={29} strokeWidth={1.4}/></span><span className="eyebrow">STO. NIÑO · CALAPAN CITY</span><h3>A warm welcome.<br/>Right this way.</h3><p>Filipiniana Hotel & Convention Center<br/>M. Roxas Street, Oriental Mindoro</p><a className="button cream-button" href={h.maps} target="_blank" rel="noreferrer" onClick={()=>track('map_click')}>Open in Google Maps <ArrowUpRight size={16}/></a><span className="map-caption">For your exact route and current travel times.</span></Reveal></div><div className="faq-section" id="faqs"><div><Heading label="A FEW THINGS YOU MAY BE WONDERING" title={<>Let’s make your<br/>plans <em>easier.</em></>}/><button className="policies-link" onClick={openPolicies}>Booking, pool & privacy policies</button></div><Reveal className="faq-list">{content.faqs.map(f=><details key={f.question}><summary>{f.question}</summary><p>{f.answer}</p></details>)}</Reveal></div></div></section>}
